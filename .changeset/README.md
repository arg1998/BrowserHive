# Changesets

Run `bunx changeset` in a PR that changes user-facing behavior of `browserhive`. The four workspace
packages form one fixed version group; name `browserhive` in the changeset. See `specs/07-branching-and-git.md` §4.
